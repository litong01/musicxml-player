# Development container for MySoloBand
FROM node:22-alpine

# Install git and other useful development tools
RUN apk add --no-cache git bash

# Set working directory
WORKDIR /workspace

# Default command - keep container running
CMD ["sleep", "infinity"]
